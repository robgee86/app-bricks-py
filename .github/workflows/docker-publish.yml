name: RELEASE - Build & Publish Release

run-name: "Release ${{ inputs.tag || github.ref_name }}"

on:
  push:
    tags:
      - "ai/*"
      - "bricks/*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Tag to release (e.g. bricks/1.2.3) — the prefix is the containers/ group to release"
        type: string
        required: true
      skip_cache:
        description: "Rebuild without cache"
        type: boolean
        required: false
        default: false

env:
  GHCR_REGISTRY: ghcr.io
  GHCR_DESTINATION_ORG: ghcr.io/${{ github.repository_owner }}
  GH_DESTINATION_REPO: https://github.com/${{ github.repository }}

permissions:
  contents: write
  packages: write
  id-token: write
  pull-requests: write
  actions: write

jobs:
  detect:
    runs-on: ubuntu-24.04-arm
    outputs:
      tag_ref: ${{ steps.detect.outputs.tag_ref }}
      group: ${{ steps.detect.outputs.group }}
      version: ${{ steps.detect.outputs.version }}
      prerelease: ${{ steps.detect.outputs.prerelease }}
      pinned_images: ${{ steps.detect.outputs.pinned_images }}
      level_0: ${{ steps.detect.outputs.level_0 }}
      level_1: ${{ steps.detect.outputs.level_1 }}
      level_2: ${{ steps.detect.outputs.level_2 }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Detect containers to release
        id: detect
        run: |
          if [[ "${{ github.event_name }}" == "workflow_dispatch" ]]; then
            TAG_REF="${{ inputs.tag }}"
          else
            TAG_REF="${GITHUB_REF_NAME}"
          fi

          VERSION="${TAG_REF#*/}"
          echo "tag_ref=$TAG_REF" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

          if [[ "$VERSION" =~ rc|alpha|beta ]]; then
            echo "prerelease=true" >> $GITHUB_OUTPUT
          else
            echo "prerelease=false" >> $GITHUB_OUTPUT
          fi

          # The tag prefix IS the containers/ group to release (ai/0.12.0 ->
          # containers/ai/). Seed the build plan with that group, then pull in
          # all descendants and ancestors and split the result into topological
          # waves (level_0 .. level_2).
          GROUP="${TAG_REF%%/*}"
          echo "group=$GROUP" >> $GITHUB_OUTPUT
          echo "Releasing group: containers/${GROUP}/"
          python3 -m scripts.build_levels --mode release --group "$GROUP"

          # Only the bricks release assembles sboms.zip. Besides its own build
          # set, scanned wave by wave, it must cover the images it ships without
          # building them: the runners pinned by the compose files and their bases.
          if [[ "$GROUP" == "bricks" ]]; then
            PINNED=$(python3 -m scripts.distributed_images --group "$GROUP" --version "$VERSION" --pinned-only --format json)
          else
            PINNED="[]"
          fi
          echo "pinned_images=$PINNED" >> $GITHUB_OUTPUT

  upload-whl:
    runs-on: ubuntu-24.04-arm
    needs: [detect]
    env:
      PYTHON_VERSION: "3.13"
      TASKFILE_VERSION: "3.45.4"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Build wheel in debian:trixie
        if: startsWith(needs.detect.outputs.tag_ref, 'bricks/')
        run: |
          # Remove tags that setuptools_scm can't parse (see tag_regex in pyproject.toml:
          # bricks/* is the current library release prefix, release/* the historical one)
          git tag -l | grep -v -E '^(ai|bricks|release)/' | xargs -r git tag -d

          # Explicitly set the version so setuptools_scm does not need the git tag to exist
          # (handles workflow_dispatch runs where no tag has been pushed yet)
          docker run --rm \
            -v "${{ github.workspace }}:/${{ github.workspace }}" \
            -w "${{ github.workspace }}" \
            -e SOURCE_DATE_EPOCH="$(git log -1 --format=%ct)" \
            -e PUBLIC_IMAGE_REGISTRY_BASE="${{ env.GHCR_DESTINATION_ORG }}/" \
            -e SETUPTOOLS_SCM_PRETEND_VERSION="${{ needs.detect.outputs.version }}" \
            debian:trixie bash -c "
              set -e
              apt-get update
              apt-get install -y --no-install-recommends --no-install-suggests sudo git ca-certificates python3-dev python3-pip build-essential pkg-config
              echo '[global]' > /etc/pip.conf
              echo 'break-system-packages = true' >> /etc/pip.conf
              git config --global --add safe.directory '*'
              pip install go-task-bin==${{ env.TASKFILE_VERSION }} uv==0.10.3
              task init:ci
              task build
              cp pyproject.toml uv.lock dist/
            "

      - name: Share wheel and lock with downstream jobs
        uses: actions/upload-artifact@v4
        with:
          name: whl
          path: |
            dist/arduino*.whl
            dist/pyproject.toml
            dist/uv.lock
          retention-days: 1

  build-l0:
    needs: [detect, upload-whl]
    if: needs.detect.outputs.level_0 != '[]'
    uses: ./.github/workflows/_build-containers-release.yml
    secrets: inherit
    with:
      containers: ${{ needs.detect.outputs.level_0 }}
      version: ${{ needs.detect.outputs.version }}
      tag_ref: ${{ needs.detect.outputs.tag_ref }}
      prerelease: ${{ needs.detect.outputs.prerelease }}
      allow_retag: true
      skip_cache: ${{ inputs.skip_cache || false }}

  build-l1:
    needs: [detect, upload-whl, build-l0]
    if: needs.detect.outputs.level_1 != '[]'
    uses: ./.github/workflows/_build-containers-release.yml
    secrets: inherit
    with:
      containers: ${{ needs.detect.outputs.level_1 }}
      version: ${{ needs.detect.outputs.version }}
      tag_ref: ${{ needs.detect.outputs.tag_ref }}
      prerelease: ${{ needs.detect.outputs.prerelease }}
      allow_retag: false
      skip_cache: ${{ inputs.skip_cache || false }}

  build-l2:
    needs: [detect, upload-whl, build-l1]
    if: needs.detect.outputs.level_2 != '[]'
    uses: ./.github/workflows/_build-containers-release.yml
    secrets: inherit
    with:
      containers: ${{ needs.detect.outputs.level_2 }}
      version: ${{ needs.detect.outputs.version }}
      tag_ref: ${{ needs.detect.outputs.tag_ref }}
      prerelease: ${{ needs.detect.outputs.prerelease }}
      allow_retag: false
      skip_cache: ${{ inputs.skip_cache || false }}

  # SBOM scans run one wave behind the builds: each wave is scanned as soon as it
  # is pushed, in parallel with the next wave's build. The images pinned by the
  # compose files were published by an earlier release, so their scan starts
  # right away. Scans never fail the workflow run: a missing SBOM is then reported
  # by upload-release.
  sbom-pinned:
    needs: [detect]
    if: needs.detect.outputs.pinned_images != '[]'
    uses: ./.github/workflows/_sbom-wave.yml
    with:
      images: ${{ needs.detect.outputs.pinned_images }}

  sbom-l0:
    needs: [detect, build-l0]
    if: needs.detect.outputs.level_0 != '[]' && startsWith(needs.detect.outputs.tag_ref, 'bricks/')
    uses: ./.github/workflows/_sbom-wave.yml
    with:
      images: ${{ needs.detect.outputs.level_0 }}
      version: ${{ needs.detect.outputs.version }}

  sbom-l1:
    needs: [detect, build-l1]
    if: needs.detect.outputs.level_1 != '[]' && startsWith(needs.detect.outputs.tag_ref, 'bricks/')
    uses: ./.github/workflows/_sbom-wave.yml
    with:
      images: ${{ needs.detect.outputs.level_1 }}
      version: ${{ needs.detect.outputs.version }}

  sbom-l2:
    needs: [detect, build-l2]
    if: needs.detect.outputs.level_2 != '[]' && startsWith(needs.detect.outputs.tag_ref, 'bricks/')
    uses: ./.github/workflows/_sbom-wave.yml
    with:
      images: ${{ needs.detect.outputs.level_2 }}
      version: ${{ needs.detect.outputs.version }}

  upload-release:
    runs-on: ubuntu-24.04-arm
    # Skipped waves (and their scans) must not skip this job, failed builds must.
    if: ${{ !failure() && !cancelled() && startsWith(needs.detect.outputs.tag_ref, 'bricks/') }}
    needs: [detect, upload-whl, build-l0, build-l1, build-l2, sbom-pinned, sbom-l0, sbom-l1, sbom-l2]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Download wheel artifact
        uses: actions/download-artifact@v4
        with:
          name: whl
          path: dist/

      - name: Download SBOM artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: sbom-delta-*
          path: sbom-delta/

      - name: Assemble sboms.zip
        env:
          GROUP: ${{ needs.detect.outputs.group }}
          VERSION: ${{ needs.detect.outputs.version }}
        run: |
          shopt -s nullglob
          mkdir -p sbom-delta

          # Artifacts are named sbom-delta-<name>-<version>: strip the prefix so the
          # archive holds one <name>-<version>/ folder per image.
          for dir in sbom-delta/sbom-delta-*/; do
            mv "$dir" "sbom-delta/$(basename "$dir" | sed 's/^sbom-delta-//')"
          done

          # Every distributed image must be covered. A missing SBOM is a warning,
          # not a blocker: it is listed in the archive and in the job summary.
          MISSING=()
          while IFS= read -r IMAGE; do
            [[ -f "sbom-delta/${IMAGE/:/-}/delta.spdx.json" ]] || MISSING+=("$IMAGE")
          done < <(python3 -m scripts.distributed_images --group "$GROUP" --version "$VERSION")

          if (( ${#MISSING[@]} )); then
            printf '%s\n' "${MISSING[@]}" > sbom-delta/MISSING.txt
            for IMAGE in "${MISSING[@]}"; do
              echo "::warning title=SBOM missing::$IMAGE has no SBOM in sboms.zip, see the sbom-* jobs"
            done
            { echo "## SBOMs missing from sboms.zip"; printf -- '- %s\n' "${MISSING[@]}"; } >> "$GITHUB_STEP_SUMMARY"
          fi

          zip -r dist/sboms.zip sbom-delta/

      - name: Upload release asset
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/arduino*.whl
            dist/sboms.zip
          tag_name: ${{ needs.detect.outputs.tag_ref }}
          name: Release ${{ needs.detect.outputs.version }}
          prerelease: ${{ needs.detect.outputs.prerelease }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
