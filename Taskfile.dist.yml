version: "3"

tasks:
  init:
    desc: Set up the development environment, uv provides Python 3.13 and installs everything into .venv
    cmds:
      - uv sync --extra dev

  init:ci:
    desc: Initialize CI environment
    cmds:
      - apt-get update
      - apt-get install -y --no-install-recommends --no-install-suggests libasound2-dev portaudio19-dev libzbar0
      - apt-get install -y --no-install-recommends --no-install-suggests gh git ca-certificates build-essential pkg-config
      - apt-get install -y --no-install-recommends --no-install-suggests libavcodec-dev libavformat-dev libavutil-dev libswscale-dev
      - apt-get install -y --no-install-recommends --no-install-suggests libjpeg62-turbo-dev libpng-dev libtiff-dev libwebp-dev libopenblas-dev liblapacke-dev
      - apt-get install -y --no-install-recommends --no-install-suggests libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev
      - task: init

  deps:lock:
    desc: Refresh uv.lock for the library and every container, pass -- --upgrade to move to newer versions
    cmds:
      - uv lock {{.CLI_ARGS}}
      - for p in containers/*/*/pyproject.toml; do uv lock --project "$(dirname "$p")" {{.CLI_ARGS}}; done

  doc:
    desc: Generate documentation
    cmds:
      - uv run python -m docs_generator.runner

  test:
    desc: Run all unit tests
    cmds:
      - uv run pytest -vs ./tests --ignore=tests/arduino/app_bricks/cloud_llm/eval/

  llm_test:
    desc: Run all LLM tests
    cmds:
      - uv run deepeval test run tests/arduino/app_bricks/cloud_llm/eval/ -n 4 -vs

  test:*:
    desc: Run tests for a specific folder
    cmds:
      - uv run pytest -vs ./tests/{{index .MATCH 0}}

  lint:
    desc: Run linters
    cmds:
      - uv run ruff check --fix .

  check:examples-alignment:
    desc: "Check app-bricks-examples alignment against this library (see scripts/check_examples_alignment.py --help)"
    cmds:
      - uv run python scripts/check_examples_alignment.py {{.CLI_ARGS}}

  check:examples-alignment:run:
    desc: Run pyright over the app-bricks-examples code against this checkout
    cmds:
      - uv run python scripts/check_examples_alignment.py run {{.CLI_ARGS}}

  check:examples-alignment:coverage:
    desc: Report the bricks that have no examples in app-bricks-examples
    cmds:
      - uv run python scripts/check_examples_alignment.py coverage {{.CLI_ARGS}}

  fmt:
    desc: Format code
    cmds:
      - uv run ruff format

  build-dev:
    desc: Build a development package
    cmds:
      - rm -rf dist
      - task doc
      - uv run python -m build --config-setting "build_type=dev" .

  build:
    desc: Build a production package
    cmds:
      - rm -rf dist
      - task doc
      - uv run python -m build .

  license:headers:
    desc: Update SPDX license headers (REUSE)
    cmds:
      - |
        git ls-files |
        grep -Ev "(^|/)(docs|examples-internal|tools)/|\.(md|txt|json|yml|yaml|toml|gitignore|dockerignore|gitmodules|in|ini|eim|lock)$" |
        sed 's/ /\\ /g' |
        xargs uv run reuse annotate -r --skip-unrecognized --copyright-prefix 'spdx-string-c' --copyright 'Arduino s.r.l. and/or its affiliated companies' --exclude-year --license 'MPL-2.0'
      - uv run reuse download --all
      - |
        licenses_dir="LICENSES"
        if [ -d "$licenses_dir" ]; then
          if [ -f "$licenses_dir/MPL-2.0.txt" ]; then
            mv "$licenses_dir/MPL-2.0.txt" LICENSE.txt
          fi
          rm -rf "$licenses_dir"
        fi
      # Strip trailing whitespace from license files.
      # The upstream SPDX license texts (fetched by `reuse download`)
      # contain stray trailing spaces that break license parsers such
      # as `licensed`.
      - cmd: find . -maxdepth 1 -name 'LICENSE*' -type f -exec sed -i '' 's/[[:space:]]*$//' {} +
        platforms: [darwin]
      - cmd: find . -maxdepth 1 -name 'LICENSE*' -type f -exec sed -i 's/[[:space:]]*$//' {} +
        platforms: [linux, windows]

  license:deps:
    desc: Check and cache dependency licenses (licensed, via Docker)
    cmds:
      - task: license:deps:build
      - task: license:deps:run

  license:deps:build:
    desc: Build the licensed image
    cmds:
      - docker build -t arduino-licensed scripts/licensed

  license:deps:run:
    desc: Run the licensed scan, LICENSED_VENVS is the volume or host directory holding the venvs
    vars:
      LICENSED_VENVS: '{{.LICENSED_VENVS | default "arduino-licensed-venvs"}}'
    cmds:
      - docker run --rm -v "$(pwd):/src" -v "{{.LICENSED_VENVS}}:/venvs" arduino-licensed

  license:deps:clean:
    desc: Remove the venvs cached by the licensed scan, the image itself is kept
    cmds:
      - docker volume rm -f arduino-licensed-venvs

  license:
    desc: Update license headers, files, and dependency license caches
    cmds:
      - task: license:headers
      - task: license:deps

  sbom:generate:internal:
    internal: true
    cmds:
      - |
        if [ -z "{{.IMAGE}}" ] || [ -z "{{.TAG}}" ] || [ -z "{{.FILENAME}}" ]; then
          echo "Error: IMAGE, TAG, and FILENAME must be set."
          exit 1
        fi

        docker sbom "{{.IMAGE}}:{{.TAG}}" --output "{{.FILENAME}}" --format spdx-json

  sbom:update:internal:
    internal: true
    cmds:
      - task: sbom:generate:internal
        vars:
          IMAGE: '{{.IMAGE}}'
          TAG: '{{.TAG}}'
          FILENAME: '{{.DIR}}/sbom_new.json'
      - |
        NEW_SBOM="{{.DIR}}/sbom_new.json"
        TMP_FILE="{{.DIR}}/sbom_tmp.json"
        CURRENT_SBOM="{{.DIR}}/sbom.spdx.json"

        jq --indent 1 --argfile current "$CURRENT_SBOM" '
          .packages |= map(
            . as $outer
            | if $outer.licenseDeclared == "NONE" or $outer.licenseDeclared == "NOASSERTION" then
                (
                 [$current.packages[]
                    | select(.name == $outer.name and .versionInfo == $outer.versionInfo)
                  ] | first
                ) as $matched
                | if $matched != null and $matched.licenseDeclared != null then
                    $outer 
                    + {licenseDeclared: $matched.licenseDeclared} 
                    + (if $matched.comment != null then {comment: $matched.comment} else {} end)
                  else
                    $outer
                  end
              else
                $outer
              end
          )
        ' "$NEW_SBOM" > "$TMP_FILE"

        cp "$TMP_FILE" "$CURRENT_SBOM"
        rm -f "$NEW_SBOM" "$TMP_FILE"

  sbom:
    desc: "Update SBOM files for python-apps-base and ei-models-runner containers. Parameters: BRICKS_TAG and EI_TAG."
    cmds:
      - task: sbom:update:internal
        vars:
          IMAGE: 'public.ecr.aws/arduino/app-bricks/python-apps-base'
          TAG: '{{.BRICKS_TAG}}'
          DIR: './containers/bricks/python-apps-base'
      - task: sbom:update:internal
        vars:
          IMAGE: 'public.ecr.aws/arduino/app-bricks/ei-models-runner'
          TAG: '{{.EI_TAG}}'
          DIR: './containers/ai/ei-models-runner'

  sbom:delta:
    desc: Generate delta SBOMs for containers (requires syft and registry access)
    cmds:
      - uv run python ./scripts/sbom_delta.py {{.CLI_ARGS}}
