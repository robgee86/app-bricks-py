[pytest]
markers =
    unit: mark a test as a unit test.
    integration: mark a test as an integration test.

# Asyncio options (commented out - uncomment if using pytest-asyncio)
# asyncio_mode = auto
# asyncio_default_fixture_loop_scope = function
